# Admin Data Session

This session handles admin data pages and data management functionality.

## Structure

- `session/` – Entry pages and session-level logic
- `ui/pages/` – Page-level widgets
- `ui/widgets/` – Reusable widgets
- `shared/` – Models and utilities shared within the session
- `business/` – Business logic and state management

## Moved Files

- `lib/pages/data_open_page.dart` -> `session/data_open_page.dart`
